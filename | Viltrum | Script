local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

local connections = {}
local bodyParts = {"Head", "Torso", "Left Arm", "Right Arm", "Left Leg", "Right Leg"}
local selectedPart = "Torso"

-- GUI SETUP
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "CombatHelper_V11"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 200, 0, 260)
MainFrame.Position = UDim2.new(0.1, 0, 0.4, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Active = true
MainFrame.Draggable = true

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(0.8, 0, 0.12, 0)
Title.Text = "FISTS HELPER V11"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.BackgroundColor3 = Color3.fromRGB(35, 35, 35)

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0.2, 0, 0.12, 0)
CloseBtn.Position = UDim2.new(0.8, 0, 0, 0)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
CloseBtn.TextColor3 = Color3.new(1, 1, 1)

-- MULTIPLIER SECTION (LIMIT REMOVED)
local MultInput = Instance.new("TextBox", MainFrame)
MultInput.Size = UDim2.new(0.35, 0, 0.15, 0)
MultInput.Position = UDim2.new(0.05, 0, 0.15, 0)
MultInput.PlaceholderText = "Mult"
MultInput.Text = "2"
MultInput.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
MultInput.TextColor3 = Color3.new(1, 1, 1)

local MultToggle = Instance.new("TextButton", MainFrame)
MultToggle.Size = UDim2.new(0.5, 0, 0.15, 0)
MultToggle.Position = UDim2.new(0.45, 0, 0.15, 0)
MultToggle.Text = "MULT: OFF"
MultToggle.BackgroundColor3 = Color3.fromRGB(70, 0, 100)
MultToggle.TextColor3 = Color3.new(1, 1, 1)

local ToggleBtn = Instance.new("TextButton", MainFrame)
ToggleBtn.Size = UDim2.new(0.9, 0, 0.15, 0)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.33, 0)
ToggleBtn.Text = "AUTO-HIT: OFF"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
ToggleBtn.TextColor3 = Color3.new(1, 1, 1)

local CleanBtn = Instance.new("TextButton", MainFrame)
CleanBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
CleanBtn.Position = UDim2.new(0.05, 0, 0.52, 0)
CleanBtn.Text = "Clean All 'Zones'"
CleanBtn.BackgroundColor3 = Color3.fromRGB(0, 70, 150)
CleanBtn.TextColor3 = Color3.new(1, 1, 1)

local DropdownBtn = Instance.new("TextButton", MainFrame)
DropdownBtn.Size = UDim2.new(0.9, 0, 0.12, 0)
DropdownBtn.Position = UDim2.new(0.05, 0, 0.68, 0)
DropdownBtn.Text = "Target: " .. selectedPart
DropdownBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
DropdownBtn.TextColor3 = Color3.new(1, 1, 1)

local DropdownFrame = Instance.new("ScrollingFrame", MainFrame)
DropdownFrame.Size = UDim2.new(0.9, 0, 0.4, 0)
DropdownFrame.Position = UDim2.new(0.05, 0, 0.85, 0)
DropdownFrame.Visible = false
DropdownFrame.CanvasSize = UDim2.new(0, 0, 2, 0)
DropdownFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
DropdownFrame.ZIndex = 5
Instance.new("UIListLayout", DropdownFrame)

-- LOGIC VARIABLES
local autoHitActive = false
local multActive = false
local isBlocking = false
local lastSpecial = 0

-- UI CONNECTIONS
for _, partName in pairs(bodyParts) do
    local b = Instance.new("TextButton", DropdownFrame)
    b.Size = UDim2.new(1, 0, 0, 25)
    b.Text = partName
    b.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    b.TextColor3 = Color3.new(1, 1, 1)
    b.ZIndex = 6
    b.MouseButton1Click:Connect(function()
        selectedPart = partName
        DropdownBtn.Text = "Target: " .. partName
        DropdownFrame.Visible = false
    end)
end

DropdownBtn.MouseButton1Click:Connect(function() DropdownFrame.Visible = not DropdownFrame.Visible end)
ToggleBtn.MouseButton1Click:Connect(function()
    autoHitActive = not autoHitActive
    ToggleBtn.Text = "AUTO-HIT: " .. (autoHitActive and "ON" or "OFF")
    ToggleBtn.BackgroundColor3 = autoHitActive and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(120, 0, 0)
end)
MultToggle.MouseButton1Click:Connect(function()
    multActive = not multActive
    MultToggle.Text = "MULT: " .. (multActive and "ON" or "OFF")
    MultToggle.BackgroundColor3 = multActive and Color3.fromRGB(180, 0, 255) or Color3.fromRGB(70, 0, 100)
end)
CleanBtn.MouseButton1Click:Connect(function()
    local count = 0
    for _, obj in pairs(game.Workspace:GetDescendants()) do
        if (obj:IsA("BasePart") or obj:IsA("Model")) and string.find(string.lower(obj.Name), "zone") then
            obj:Destroy()
            count = count + 1
        end
    end
    CleanBtn.Text = "Deleted " .. count .. " Zones!"
    task.delay(1.5, function() CleanBtn.Text = "Clean All 'Zones'" end)
end)
CloseBtn.MouseButton1Click:Connect(function()
    for _, conn in pairs(connections) do conn:Disconnect() end
    ScreenGui:Destroy()
end)

-- TARGETING
local function getBestTarget()
    local target = Mouse.Target
    if target and target.Parent and target.Parent:FindFirstChild("Humanoid") then return target.Parent end
    local closest, dist = nil, 12
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local d = (player.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude
            if d < dist then dist = d closest = player.Character end
        end
    end
    return closest
end

-- ATTACK ENGINE
local function performAttack(actionType, val)
    local char = LocalPlayer.Character
    local remote = char and char:FindFirstChild("Fists") and char.Fists:FindFirstChild("fistremote")
    if not remote then return end 

    -- Filter UI
    local guiObjects = LocalPlayer.PlayerGui:GetGuiObjectsAtPosition(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)
    for _, obj in pairs(guiObjects) do if obj:IsDescendantOf(MainFrame) then return end end

    -- Multiplier Logic (No Limit)
    local count = (actionType == "lmb" and multActive) and (tonumber(MultInput.Text) or 1) or 1

    -- Fire Swing
    remote:FireServer(actionType, val)

    -- Fire Damage
    if autoHitActive and not isBlocking then
        local targetChar = getBestTarget()
        if targetChar then
            local targetPart = targetChar:FindFirstChild(selectedPart)
            if targetPart then
                for i = 1, count do
                    remote:FireServer("part", targetPart, targetPart.Position, Vector3.new(0, 0, 0))
                    -- No delay between packets for instant damage burst
                end
            end
        end
    end
end

-- INPUTS
table.insert(connections, UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        performAttack("lmb")
    elseif input.KeyCode == Enum.KeyCode.Q or input.KeyCode == Enum.KeyCode.E then
        if tick() - lastSpecial > 0.5 then
            lastSpecial = tick()
            performAttack("keypress", input.KeyCode == Enum.KeyCode.Q and "q" or "e")
        end
    elseif input.KeyCode == Enum.KeyCode.F then
        isBlocking = true
        performAttack("keypress", "f")
    elseif input.KeyCode == Enum.KeyCode.R then
        performAttack("keypress", "r")
    end
end))

table.insert(connections, UserInputService.InputEnded:Connect(function(input, gpe)
    if input.KeyCode == Enum.KeyCode.F then isBlocking = false end
end))
