--[[ 
    DRAGGABLE ITEM TRACKER & BANKNOTE TP
    Instructions: Drag the "ITEM TRACKER" title bar to move the UI.
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Player = Players.LocalPlayer

-- Configuration
local BoxSize = Vector3.new(867, 89, 783)
local BoxPos = Vector3.new(5662.36, 720.27, -1317.86)
local CollectNotes = false

-- Helper: Get Position
local function getPos(obj)
    if obj:IsA("BasePart") then return obj.Position end
    local p = obj:FindFirstChild("Handle") or obj:FindFirstChildWhichIsA("BasePart") or (obj:IsA("Model") and obj.PrimaryPart)
    return p and p.Position or nil
end

-- Teleport Method
local function teleport(pos)
    local char = Player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if root and pos then
        root.CFrame = CFrame.new(pos) + Vector3.new(0, 3, 0)
    end
end

-- GUI Setup
local ScreenGui = Instance.new("ScreenGui", Player:WaitForChild("PlayerGui"))
ScreenGui.Name = "DraggableTracker"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 220, 0, 350)
MainFrame.Position = UDim2.new(0.1, 0, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0

local TitleBar = Instance.new("TextLabel", MainFrame)
TitleBar.Size = UDim2.new(1, 0, 0, 30)
TitleBar.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
TitleBar.Text = "ITEM TRACKER (DRAG ME)"
TitleBar.TextColor3 = Color3.new(1, 1, 1)
TitleBar.Font = Enum.Font.SourceSansBold

local NoteBtn = Instance.new("TextButton", MainFrame)
NoteBtn.Size = UDim2.new(0.9, 0, 0, 35)
NoteBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
NoteBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
NoteBtn.Text = "Collect Notes: OFF"
NoteBtn.TextColor3 = Color3.new(1, 1, 1)

local ListContainer = Instance.new("ScrollingFrame", MainFrame)
ListContainer.Size = UDim2.new(1, 0, 1, -80)
ListContainer.Position = UDim2.new(0, 0, 0, 80)
ListContainer.BackgroundTransparency = 1
Instance.new("UIListLayout", ListContainer)

-- [[ DRAG LOGIC ]] --
local dragging, dragInput, dragStart, startPos

TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Toggle Collector
NoteBtn.MouseButton1Click:Connect(function()
    CollectNotes = not CollectNotes
    NoteBtn.Text = CollectNotes and "Collect Notes: ON" or "Collect Notes: OFF"
    NoteBtn.BackgroundColor3 = CollectNotes and Color3.fromRGB(50, 150, 50) or Color3.fromRGB(150, 50, 50)
end)

-- Main Dynamic Loop
task.spawn(function()
    while task.wait(0.8) do
        local counts = {}
        local firstPos = {}

        for _, v in pairs(ListContainer:GetChildren()) do
            if v:IsA("TextButton") then v:Destroy() end
        end

        for _, item in pairs(workspace:GetDescendants()) do
            -- Banknote Check
            if CollectNotes and (item.Name == "Banknote" or item.Name == "Bank Note") then
                local p = getPos(item)
                if p then teleport(p) end
            end

            -- Box Detection
            local p = getPos(item)
            if p then
                local min = BoxPos - (BoxSize / 2)
                local max = BoxPos + (BoxSize / 2)
                if (p.X >= min.X and p.X <= max.X) and (p.Y >= min.Y and p.Y <= max.Y) and (p.Z >= min.Z and p.Z <= max.Z) then
                    local name = item.Name
                    if item:IsA("Handle") then name = item.Parent.Name end
                    
                    counts[name] = (counts[name] or 0) + 1
                    if not firstPos[name] then firstPos[name] = p end
                end
            end
        end

        for name, count in pairs(counts) do
            local b = Instance.new("TextButton", ListContainer)
            b.Size = UDim2.new(1, 0, 0, 25)
            b.BackgroundTransparency = 0.9
            b.Text = name .. " x" .. count
            b.TextColor3 = Color3.new(1, 1, 1)
            b.MouseButton1Click:Connect(function() teleport(firstPos[name]) end)
        end
    end
end)
